# Deployed Streamlit app — slim runtime deps only (the app does NOT import
# xgboost / scikit-learn / shap / openmeteo; those are offline-only, see requirements-dev.txt).
streamlit
streamlit-folium
folium
numpy
pandas
plotly
numpy-financial
